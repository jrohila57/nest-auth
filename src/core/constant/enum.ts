export enum Role {
  Admin = 'Admin',
  User = 'User',
}

export enum Gender {
  Male = 'male',
  Female = 'female',
  Other = 'other',
}

export enum Type {
  RESET = 'RESET',
  FORGOT = 'FORGOT',
  VERIFY = 'VERIFY',
}
